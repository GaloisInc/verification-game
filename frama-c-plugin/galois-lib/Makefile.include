
GALOIS_LIBDIR   = ${PLUGIN_DIR}/../galois-lib

EXTRA_FLAGS     = -I ${GALOIS_LIBDIR}
PLUGIN_OFLAGS   += ${EXTRA_FLAGS}
PLUGIN_BFLAGS   += ${EXTRA_FLAGS}
PLUGIN_DEPFLAGS += ${EXTRA_FLAGS}
PLUGIN_CMO      := ${GALOIS_LIBDIR}/galois_lib ${GALOIS_LIBDIR}/galois_cil ${GALOIS_LIBDIR}/galois_symbols ${PLUGIN_CMO}
